import { ConteudoMedidaPipe } from './conteudo-medida.pipe';

describe('ConteudoMedidaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConteudoMedidaPipe();
    expect(pipe).toBeTruthy();
  });
});
